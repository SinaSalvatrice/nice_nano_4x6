include:
  # Standard firmware build
  - board: nice_nano_v2
    shield: nice_nano_4x6
  # ZMK Studio-enabled firmware build
  - board: nice_nano_v2
    shield: nice_nano_4x6
    snippet: studio-rpc-usb-uart
